# Used for production builds (`pnpm build`). Local `vite dev` calls `/api` on the same port and
# proxies to the backend (see vite.config); optional override:
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:3001
VITE_API_URL=http://localhost:3001
